# 用户管理前端

用户管理页面位于 `/users`，使用现有后端接口。

## 页面行为

- 用户列表只显示用户名，移除额外显示名称与备注标记；新增和编辑抽屉均不提供显示名称输入，编辑抽屉移除备注及第三方绑定身份信息。
- 新建用户仍将用户名作为兼容接口所需的显示名称。编辑已有用户时，内部保留加载到的原显示名称、备注，不因隐藏字段而清空原数据；第三方绑定关系不变。
- 用户资料加载完成前，禁止保存和编辑额度，避免使用尚未加载的默认数据。
- 编辑抽屉直接提供可输入的剩余额度，默认填入当前余额；移除“调整额度”按钮和二次弹窗。点击现有“保存更改”时统一保存，输入或失焦时不写入数据库。
- 只有额度发生变化时才调用 `/api/user/manage`，固定提交 `action: add_quota`、`mode: override`，覆盖余额而非累加或扣减；未修改额度时不覆盖余额。仅修改额度时不调用用户资料更新接口。
- 额度可显式设置为 0；空值、负数、非有限数及换算后超过 JavaScript 安全整数范围的值不能提交。金额沿用系统货币、汇率或 Token 展示配置进行换算。
- 用户资料与额度使用现有两个接口依次保存，并非同一数据库事务。若资料已保存而额度更新失败，提示部分成功并保留额度输入；再次保存只重试尚未保存的变更，不重复提交已保存的密码等信息。
- 额度更新失败时保留输入供重试；取消并重新打开时重新加载当前余额。提交期间禁止重复提交和关闭。
- 用户操作菜单只保留启用/禁用状态切换和删除；移除升降级、绑定管理、订阅管理、重置 Passkey 和重置 2FA 入口。Root 账号仍禁止禁用和删除，删除仍需确认。独立编辑按钮保留。
- 用户列表移除邀请信息和分组列，编辑抽屉移除分组选择。编辑保存时将用户分组设为 `default`；即使其他字段没有变化，也会保存旧用户的非默认分组。额度仍仅在发生变化时覆盖。

## 默认分组与访问配置

所有变更均由前端使用现有接口完成。打开页面或关闭未保存的表单不会修改数据；未编辑的旧记录保留原配置，不执行数据库批量迁移。

| 对象 | 新建或编辑保存时的配置 |
| --- | --- |
| 用户 | 默认分组 `default`；新建沿用后端现有默认分组 |
| API 密钥（仅新建） | `unlimited_quota: true`，`remain_quota: 0`，`group: default`，`auto_groups: []`，`cross_group_retry: false`；无 IP、模型白名单，`expired_time: -1` 永不过期 |
| 渠道 | `group: default`，`priority: 0`，`weight: 0`；标签批量编辑同样提交这些值 |
| 订阅方案 | 不再升级用户分组，到期降级目标固定为 `default` |
| 在线试用 | 模型查询和请求固定使用 `default`，忽略历史非默认分组选择 |

- API 密钥使用居中创建弹窗，仅保留名称输入框，列表移除额度、分组、白名单和过期信息。新密钥不限额度且永不过期；不再提供编辑入口，旧密钥的额度、过期时间和其他限制不做迁移。
- 渠道表格、卡片、编辑抽屉、标签编辑与批量编辑移除分组、优先级和权重入口；同时移除模型预设分组快捷入口。
- 日志筛选与详情、价格列表与详情、个人信息及订阅展示中的用户分组信息一并清理。价格列表使用默认分组，设置导航移除分组计费，限流与渠道亲和性表单移除分组控件。
- 后端分组倍率与权限校验继续使用现有配置。本次隐藏对应页面入口，不会修改后端计费规则、数据库结构或未编辑的历史数据。

页面改动仅涉及前端，未修改后端代码、数据库结构或账号绑定。删除入口不等于删除后端功能。测试数据的写入范围见下文。

## 上一轮验证

2026-09-21，在 `web/` 中完成：

- `npm run typecheck`：通过。
- 修改的 TypeScript 文件与新增测试通过 oxlint；格式化保留原版权头。
- `npm run test -- src/features/users src/features/dashboard/components/users/__tests__/user-analytics.test.tsx --maxWorkers=2`：7 个文件、40 个测试通过，覆盖点击保存才写入、零值、非法值、取消不保存、未改额度不覆盖、部分失败重试、货币换算、重新打开、加载保护、原资料保留、新建兼容、菜单和删除确认，以及用户统计回归。
- `npm run build`：通过，产物位于 `web/dist`。
- 额度说明、校验文案及部分保存失败提示通过翻译脚本写入全部七种语言，并执行 i18n 同步。
- 本地浏览器核对编辑抽屉：额度可直接输入且预填当前余额，无“调整额度”按钮；输入 0 后关闭再打开恢复原余额，未提交实际余额修改、禁用或删除。

预览地址为 `http://localhost:3001/users`。未部署本机 3000 服务或远程站点。临时翻译、格式化脚本与同步报告已清理。

## 本轮验证：默认配置精简

2026-09-21，在 `web/` 中完成：

- `npm run typecheck`、所有修改文件的 oxlint、`git diff --check`：通过。
- `npm run test -- src/features/users src/features/keys src/features/channels/lib/__tests__ src/features/usage-logs src/features/pricing src/features/playground/hooks/use-stream-request.test.ts --maxWorkers=2`：31 个文件、185 项测试通过。
- 回归覆盖隐藏列与表单字段、密钥批量创建和旧限制清除、加载失败时禁止保存、用户默认分组、渠道创建与更新默认路由，以及既有额度、日志、价格与请求流程。
- `npm run build`：通过；本地 3001 预览已更新。
- 浏览器核对用户表格及编辑抽屉、密钥表格及创建表单、渠道卡片及创建抽屉（包含展开后的高级设置）；只打开和取消表单，未提交业务数据修改。
- 新文案已写入七种语言并完成 i18n 同步。未修改后端、未部署远程。

## API 密钥操作精简与 Mock 数据（2026-09-21）

- 每个密钥仅提供复制、停用和删除；停用后不提供重新启用入口。复制按需获取完整密钥，列表仅展示掩码；删除需确认。
- 删除编辑、额度、完整密钥弹窗、连接信息复制、CC Switch 和聊天工具入口。批量操作仅保留复制和删除。
- 状态展示与筛选仅有启用和停用。旧过期/耗尽状态统一归入停用展示，不会修改其后台状态或恢复使用。
- 通过现有接口新建密钥，固定不限额度、永不过期与默认分组。用户账户本身的余额限制继续生效。
- 创建弹窗每次只创建一个密钥；输入名称后点击确认或按回车提交。创建成功后关闭弹窗并刷新列表，失败时保留名称供重试。

### 本地测试数据

在现有 PostgreSQL 15.19 容器 `ark-seedance-gateway-postgres-1` 的 `new_api` 数据库中，为 `testuser01`～`testuser05` 写入批次 `mock-20260921-keys-usage`：

- 每人 2 个随机测试密钥（1 个启用、1 个停用），共 10 个；全部不限额度、永不过期、默认分组，名称带批次标记。
- 每人 42 条最近七天的调用记录（35 次成功、7 次失败），共 210 条；模型为 `mock-gpt-4o-mini`、`mock-image-gen`、`mock-video-gen`。
- 同步写入 210 条模型用量事件、210 条尝试记录、210 条日志、175 条小时用量汇总。测试用户和新增密钥的累计用量同步增加，已有密钥及用户余额保持原值。
- 所有数据在同一事务中写入，重复批次会被拒绝；无真实上游调用、无真实费用扣减、无后端代码或结构变更。远程数据库未修改。
- 可通过事件的 `backfill_batch`、日志的 `other.batch`/`request_id`、汇总的 `node_name` 和密钥名称定位该批次。API 密钥页面只显示当前登录用户自己的密钥；查看这五个用户的曲线请在用户统计中选择对应用户名。

### 本轮验证

- `npm run typecheck`、修改文件的 oxlint、保留版权头的定向格式化、`git diff --check`：通过。本机未提供 Bun，使用现有 npm 工具链。
- `npm run test -- src/features/keys src/features/dashboard/components/users/__tests__/user-analytics.test.tsx --maxWorkers=2`：7 个文件、40 项测试通过。覆盖无限额度请求、数量/名称校验、部分成功与失败重试、复制真实密钥、仅更新停用状态、删除确认和旧状态筛选。
- `npm run build`：通过，本地 3001 预览已更新；未发布到远程。
- 浏览器核对密钥表格、创建表单、状态筛选及用户统计；`testuser01` 显示 42 次调用、35 成功、7 失败、17,941 Token。
- 数据库核对：事件与日志均为 210 条；175 次成功汇总的额度和 Token 与事件/日志一致，分别为 134,750 额度单位和 107,905 Token。五个用户余额均保持写入前值。临时 SQL 文件已清理。

## 居中创建密钥弹窗（2026-09-21）

- 点击“创建 API 密钥”打开居中紧凑弹窗，自动聚焦名称输入框；仅保留名称、“取消”和“确认”。移除侧边抽屉、数量设置与批量创建逻辑。
- 空名称或纯空格时禁用确认，提交前去除名称两端空白。一次请求创建一个不限额度、永不过期的默认分组密钥。
- 支持回车确认、Escape 取消；取消后再次打开为空表单。请求期间禁用输入和操作，阻止重复提交及关闭。
- 成功后提示创建完成、关闭弹窗并刷新列表；可使用列表中的复制功能获取密钥。失败保留名称并允许重试。
- 沿用现有接口和七语言翻译，本次没有修改后端或写入数据库数据。

验证：密钥模块 6 个测试文件、35 项测试通过；类型检查、修改文件 lint、构建与 `git diff --check` 通过。已在本地浏览器核对弹窗居中、名称自动聚焦、空值确认禁用与紧凑布局，3001 预览已更新。本轮浏览器核对未提交实际密钥创建。
